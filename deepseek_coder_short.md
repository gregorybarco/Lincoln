# Lincoln Model Evaluation Report
Generated: 2026-07-24T04:20:39
Models: deepseek-coder:latest

## Summary

| Task | Model | Tokens/sec | VRAM delta (MB) | Processor | Tool call valid? |
|---|---|---|---|---|---|
| 1_domain_reasoning | deepseek-coder:latest | 319.4 | -4389 | 100% GPU     16384 | - |
| 2_tool_call_reliability | deepseek-coder:latest | - | - | - | ERROR: 400 Client Error: Bad Request for url: http://localhost:11434/api/chat |
| 3_python_coding | deepseek-coder:latest | 313.3 | 0 | 100% GPU     16384 | - |
| 4_long_context | deepseek-coder:latest | - | - | - | SKIPPED |
| 5_sanity_check | deepseek-coder:latest | 303.3 | 0 | 100% GPU     16384 | - |

---

## 1_domain_reasoning — Fortran / quant finance domain reasoning (PLACEHOLDER -- swap this prompt)

### deepseek-coder:latest

**Response:**
```
Firstly, let me clarify that BARCO/OptionsPricing is not an official or recognized financial technology platform like Barco (Barclays) but rather a software project created by our team at Deepseek for their AI programming assistant tasks in the field of computer science and mathematics related topics.  Hence this codebase may contain proprietary information, structures etc., that are specific to BARCO/OptionsPricing's platform or system design as well certain functionalities might not be available publicly due some dependencies on third-party libraries for different platforms like MATLAB (which we use in the project).
 
In order to compute Greeks, a finite difference method is used. The Black Scholes model assumes constant volatility and interest rate which isn't always true across all markets or scenarios but this simplifies things considerably here by avoiding complex calculations required for more complicated models like options pricing at different strikes/expiries in real-world settings such as stock trading, futures etc..
 
The finite difference method is an approximation of the derivative used to calculate Greeks. It approximates a piecewise linear function which results from taking two points on either side and computing their slope between them (the first order accurate form) for small changes in x or steps taken at endpoints, but it's not necessarily equal to actual value calculation due to finite differences methods limitations of being an approximation method.
 
Here is a simplified example: For simplicity let us assume the option price has been calculated using Black-Scholes formula and Delta (derivative) will be approximated by taking difference between two points on both sides, Vega(2nd order accurate form), Gamma((second derivative)/ (first derivatives))
 
```Fortran
      implicit none  
      
    ! Declare variables for option data: strike price and expiration date. Assumed to be in days here as input from a user or generated by marketdata source    
        real*8 :: K, T, r, sigma;                 
                     
  !Declaring the arrays of size n (number of intervals) which are used for approximation:   
       integer::n  
      parameter(n=100);                           
                                 
                          complex *16 :: delta_array(-n/2:n/2),gamma_array((-n/4):(3*n)/8,& 
                                                                &5*(/-sqrt(real(i)))**integer::j)   
      real (kind=C_double )  0.d0 :: vega;           !Declaring a variable for Vega as float or double precision    
       complex *16 :: blackscholes, dummy        ;  !Complex data type to hold Black Schole price and Delta/Gamma      values   
                                  real (kind=C_double):: hh   ,hh2;                        
                                                      interface fortran function c(r, sigma);     end      >  intergral i = 0 ;  integral j = n /18.doubler>end         >  integer k  =n/4 - (j+i)/2 >integer l=(-3)*hh;   real*kind C_double ):: dummysubrutin    
       SUBROUTINE BlackScholes( K, T , r  sigma    complex *16 :: blackscholes)      call c ...../>end          subroutine DeltaCalc (delta);         common /coeffs/ hh;             integer j ;            forall i : -n.. n do delta_array(i ) = dummy       
```  This is a simplified example and would require more details to be fully functional. The BlackScholes function, DeltaCalc subroutine are not included in the above code snippet as they depend heavily on specifics of input data like r (interest rate), sigma(volatility) etc., which I assume based only from their names and usage patterns given herein due to limitations.
```

_tokens/sec: 319.4, VRAM: 15183 -> 10794 MB, processor: 100% GPU     16384_


## 2_tool_call_reliability — Agentic tool-call schema compliance

### deepseek-coder:latest

**ERROR:** 400 Client Error: Bad Request for url: http://localhost:11434/api/chat


## 3_python_coding — General Python coding task

### deepseek-coder:latest

**Response:**
```
Here is one way how this can be done in python using builtin max, min functionalities to find minimum price at each point (time) till now along with current prices that you are iterating through list of float values for maximum drawdown calculation : 

```python
def calculate_maximum_drawdown(prices):
    """ Calculate the Maximun DrawDown from a given List Of Prices. In case if there is no data in input, then return None"""  
    
    # handle empty list edge-case by checking length of prices and returning null or default value 0 for nonexistent price lists     
    if not len(prices):        
        print("Price List Is Empty")      
        return None         
          
    max_drawdown = float('inf')     # Initialize maximum draw down as infinity.   It will be used to compare with current minimum and difference in each iteration 
                                    
                                    # If prices is a list of floats, iterate through it using enumerate     
    min_price=float("Inf")          # Start the minimal price from positive infinite value     for every loop we need first element so start at infinity      
                                  
   # Iterating over each data point in given series  (Price)        with index number and corresponding float. This will be used to access latest prices    until end of list     
                                    
                                    '''for i, price_at_tick time range(1 ,len[prices])'''     """         # To do : replace this comment by a proper loop statement          " 
             for index,(price_now) in enumerate (prices):   # iterate over list at each point of data. Include price now and its position       print(index, 'th iteration')      minima =min[minimum value ,current minimum]    """        if max < current - mini:         
              maximum=max_drawdown     return abs((price_now-mini)/mini)   # calculate the draw down at each point of data and update in loop  '''abs''' to get positive result for highest possible value (as we are interested about height from which you can lose your money, not how much loss).
              """if min > current:         mini=current    print("Min price changed")   # when new minimum is found then save it and reset maximum  drawdown"""     max_drawdown = abs((price_now-mini)/mini)      if (maximum>0):          return float(str("% .2f" %max_drawdown))+'%'
```   print("Maximun DrawDown is : ",float('%.3g') + '%'))  """To get the result in percentage format, we use str to convert it into string and then add 's'' at end of ''string.format(variable)'. This will give us a correct output for both integer number or decimal numbers."""
```

_tokens/sec: 313.3, VRAM: 10794 -> 10794 MB, processor: 100% GPU     16384_


## 4_long_context — Long-context handling

### deepseek-coder:latest

_Skipped: No --long-context-file was provided, so this task was skipped. Re-run with e.g. --long-context-file lincoln/lincoln_ollama_service.py for a real long-context test._


## 5_sanity_check — Plain conversational sanity check

### deepseek-coder:latest

**Response:**
```
A local run machine learning (ML) model is considered less secure compared with ML models hosted on the internet due to several reasons, including data security and integrity issues such as malware or hacking attempts which can compromise your personal information if used incorrectly by unauthorized individuals. Here are a few key points that make locally-run AI assistants more privacy preserving:

1. **Data Storage** - Local models run in servers on the user's machine, and they typically store data persistently even after uninstallation or rebooting of their system (this is called serverless computing). This implies less need for a centralized database to manage users’ information like namespaces with AI model weights stored separately.
   
2. **Data Encryption** - The models are encrypted using strong encryption techniques in advance during the dataset preparation phase, which protects sensitive user data from being intercepted or accessed by unauthorised individuals while they're offline (this is also known as end-to-end security). 
   
3. **Model Security and Privacy** - Local models are typically trained on a small amount of personal training examples for model optimization, which can be seen in the privacy settings under ML platforms like TensorFlow or PyTorch. This ensures that your data isn't exposed to unauthorized individuals during these steps if they were sharing with other AI assistants and you trust them not having access too much sensitive details about who is using it at any given moment (this also known as anonymity). 
   
4. **Minimal Access** - Only the necessary data for a specific task can be accessed by users, reducing their exposure to potential hacks or malicious activities that could steal user's information and privacy rights in return from misuse of your AI model services (this is also known as pseudonymization). 
   
5. **Data Anonymity** - Data anonymisation techniques are often used for this purpose, which means we mask any identifying data within the dataset to retain a level of security while making it accessible only through specific tasks or methods provided by your service (this is also known as de-identification). 
   
6. **Data Retention** - Local models have less time and resources than cloud services because they are hosted on user's machine, so there’re fewer storage costs with no longer need for data to be accessed or deleted after usage/retired (this is also known as self-purge). 
   
7. **Interoperability** - Local models can run in a variety of environments such Android and iOS apps running on smartphones, which could potentially increase the user base by increasing reach across platforms since they would be using your AI model more frequently than cloud ML services do (this is also known as multi-platform access).
```

_tokens/sec: 303.3, VRAM: 10794 -> 10794 MB, processor: 100% GPU     16384_
